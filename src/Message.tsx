// PascalCasing
function Message(){
    // JavaScript + XML
    // JSX
    const name = "";
    if(name)
        return <h1>Hello {name}</h1>;
    return <h1>Hello React</h1>;
}

export default Message;